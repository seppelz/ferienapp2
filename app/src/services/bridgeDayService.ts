import { Holiday, BridgeDay } from '../types/holiday';
import { addDays, isWeekend, isSameDay, isWithinInterval, subDays, getDay, differenceInDays, eachDayOfInterval, format, parse } from 'date-fns';
import { GermanState } from '../types/GermanState';

// ... rest of the file ... 